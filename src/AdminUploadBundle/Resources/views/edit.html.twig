{% extends 'SonataAdminBundle:CRUD:base_edit.html.twig' %}

{% block javascripts %}
    {{ parent() }}
    <script type="text/javascript" src="{{ asset('bundles/adminupload/ckeditor/ckeditor.js') }}"></script>
    <script type="text/javascript" src="{{ asset('bundles/adminupload/ckeditor/adapters/jquery.js') }}"></script>
    <script type="text/javascript">
        $(function() {
            $('textarea.editor').ckeditor({
                filebrowserUploadUrl: '{{ path('admin_upload') }}'
            });
        });
    </script>
{% endblock %}